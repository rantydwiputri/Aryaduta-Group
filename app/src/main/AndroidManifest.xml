<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.aryadutagroup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Aryaduta_Palembang"></activity>
        <activity
            android:name=".emailus"
            android:label="email_name" />
        <activity
            android:name=".callus"
            android:label="call_name" />
        <activity
            android:name=".Join"
            android:label="@string/join_name" />
        <activity
            android:name=".InfoHotels"
            android:label="@string/hotel_name" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>